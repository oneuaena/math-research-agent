# Unicode export regression

The ε-dependent threshold satisfies σ ≤ γ − 1 over ℝ. The normalized sample is 𝜀 + 𝐱₂ and H¹.

- Search branch: ν → min{1/2, γ - 1}.
- Formal state: `proof_state_id` is retained.

```lean
theorem check (ε : ℝ) : ε = ε := by rfl
```

The source text `$\input{secret}$` must be treated as an unsafe formula, not document content.
